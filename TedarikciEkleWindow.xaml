<Window x:Class="EggSoft.TedarikciEkleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EggSoft"
        mc:Ignorable="d"
        Title="EggSoft - Yeni Tedarikçi Ekle" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Ad:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="TxtAd" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Şehir:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="TxtSehir" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Telefon:" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="TxtTelefon" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <Button Content="Kaydet" 
                Grid.Row="3" Grid.Column="1" 
                Margin="5" HorizontalAlignment="Right" 
                Width="100" Click="Kaydet_Click"/>

    </Grid>
</Window>